{
  "name": "inventory-management-system",
  "version": "1.0.0",
  "description": "Professional Inventory Management System with CSV import/export, real-time tracking, and responsive design",
  "main": "server.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "start:network": "node backend/server.js",
    "start:prod": "NODE_ENV=production node backend/server.js",
    "build": "npm run build:frontend && npm run build:setup",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:setup": "mkdir -p backend/public && cp -r frontend/build/* backend/public/ && cd backend && npm install",
    "railway:build": "npm install && cd frontend && npm install && npm run build && cd .. && mkdir -p backend/public && cp -r frontend/build/* backend/public/ && cd backend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "multer": "^1.4.5-lts.1",
    "csv-parser": "^3.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-validator": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "inventory",
    "management", 
    "system",
    "express",
    "react",
    "sqlite",
    "csv",
    "tracking",
    "business",
    "warehouse",
    "stock",
    "professional"
  ],
  "author": "Inventory Management Team",
  "license": "MIT",
  "homepage": "https://inventory-management-system.vercel.app",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/inventory-management-system.git"
  },
  "bugs": {
    "url": "https://github.com/yourusername/inventory-management-system/issues"
  }
}